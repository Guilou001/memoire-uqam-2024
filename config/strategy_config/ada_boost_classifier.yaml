prediction_pipeline:
  estimator_name: "ada_boost_classifier"
  regressor_dict: {
#        'estimator': 'decision_tree_classifier',
#        'estimator_args': {
#    "criterion": "entropy",
#    "splitter": "best",
#    "max_depth": null,
#    "min_samples_split": 3,
#    "min_samples_leaf": 3,
#    "min_weight_fraction_leaf": 0.0,
#    "max_features": null,
#    "random_state": 42,
#    "max_leaf_nodes": null,
#    "min_impurity_decrease": 0.0,
#    "class_weight": null,
#    "ccp_alpha": 0.0,
#    "monotonic_cst": null,
#    },
        'n_estimators': 300,
        'learning_rate': 0.1,
        'algorithm': "SAMME",
        'random_state': 42
    }

  bayes_search_params_grid:
    n_estimators: "(50, 500)"       # Range of n_estimators to explore
    learning_rate: "(0.01, 0.3)"    # Range of learning rates to explore
    algorithm: [ "SAMME" ]  # Categorical choice for the algorithm
#    'estimator__max_depth': (1, 10) # Assuming you're using a DecisionTree as the base estimator
#    'estimator__min_samples_split': (2, 20)
#    'estimator__min_samples_leaf': (1, 10)


  grid_search_params_grid: {
    'n_estimators': [50, 100, 200],
    'learning_rate': [0.01, 0.05],
  }


analyze_strategy_returns:
  strategy_name: "Ada_Boost_Classifier"
