prediction_pipeline:
  estimator_name: "extra_trees_classifier"
  regressor_dict: {
    "n_estimators": 100,
    "criterion": "gini",
    "max_depth": 10,  # null
    "min_samples_split": 2,
    "min_samples_leaf": 1,
    "min_weight_fraction_leaf": 0.0,
    "max_features": "sqrt",
    "max_leaf_nodes": null,  # null
    "min_impurity_decrease": 0.0,
    "bootstrap": False,
    "oob_score": False,
    "n_jobs": null,  # null
    "random_state": 42,  # null
    "verbose": 0,
    "warm_start": False,
    "class_weight": null,  # null
    "ccp_alpha": 0.2,
    "max_samples": null,  # null
    "monotonic_cst": null  # null
  }
  bayes_search_params_grid:
    n_estimators: "(50, 300)"        # Range of n_estimators to explore
    criterion: [ "gini", "entropy" ]   # Categorical options for criterion
    max_depth: "(5, 20)"             # Range for max_depth, include None if you want to test unbounded depth
    min_samples_split: "(2, 10)"     # Range for min_samples_split
    min_samples_leaf: "(1, 5)"       # Range for min_samples_leaf
    max_features: [ "sqrt", "log2" ]   # Categorical options for max_features
    ccp_alpha: "(0.0, 0.5)"          # Range for complexity parameter used for Minimal Cost-Complexity Pruning
    bootstrap: [ True, False ]         # Categorical options for bootstrap

  grid_search_params_grid: {
      "n_estimators": [50, 100, 200],
      "criterion": ["gini", "entropy"],
      "max_depth": [7, 10],
      "min_samples_split": [2],
      "min_samples_leaf": [1],
      "max_features": ["log2"],
      "ccp_alpha": [0.1]
  }

analyze_strategy_returns:
  strategy_name: "Extra_Trees_Classifier"