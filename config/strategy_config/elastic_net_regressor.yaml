prediction_pipeline:
  estimator_name: "elastic_net_regressor"
  regressor_dict: {
        'alpha': 1,
        'l1_ratio': 0.5,
        'fit_intercept': True,
        'precompute': False,
        'max_iter': 3000,
        'copy_X': True,
        'tol': 0.0001,
        'warm_start': False,
        'positive': False,
        'random_state': 42,
        'selection': "cyclic"
    }

  bayes_search_params_grid:
    alpha: "(0.001, 10.0)"  # Range for alpha (regularization strength)
    l1_ratio: "(0.0, 1.0)"  # Range for l1_ratio (balance between l1 and l2 regularization)
    tol: "(0.00001, 0.01)"  # Range for tolerance of optimization

  grid_search_params_grid: {
      'alpha': [0.1, 0.5, 1],
      'l1_ratio': [0.1, 0.5, 1],
      'tol': [0.0001, 0.001, 0.01],
  }

analyze_strategy_returns:
  strategy_name: "Elastic_Net_Regressor"
